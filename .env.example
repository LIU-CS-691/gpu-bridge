# This .env.example file is sample configuration for local development. Copy this to .env and modify as needed. Do not commit .env to version control.

# GPU_TOOL configuration
GPU_TOOL_SERVER=http://localhost:8000
GPU_TOOL_TOKEN=devtoken

# Controller configuration
DATABASE_URL=sqlite+pysqlite:///./dev.db
API_TOKEN=devtoken

# Integration test configuration
GPU_BRIDGE_SERVER=http://localhost:8000
GPU_BRIDGE_TOKEN=devtoken